# Calculate area of a circle using math module

import math

# Input radius of the circle
radius = float(input("Enter the radius: "))

# Calculate area
area = math.pi * math.pow(radius, 2)

print(f"The area of the circle = {area}")

#Output:
Enter the radius: 5.1
The area of the circle = 81.71282491987051
